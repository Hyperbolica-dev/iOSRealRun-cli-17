[build-system]
requires = ["setuptools>=68", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "iosrealrun-cli"
version = "0.2.0"
description = "Route-based iOS location simulation CLI"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "coloredlogs>=15.0.1",
    "geopy>=2.4.1",
    "fastapi>=0.115.0",
    "platformdirs>=4.2.0",
    "pymobiledevice3>=11.12.4",
    "PyYAML>=6.0.1",
    "uvicorn>=0.30.0",
]

[project.scripts]
iosrealrun = "iosrealrun.cli:main"
iosrealrun-ui = "iosrealrun.web:main"

[tool.setuptools]
include-package-data = true
py-modules = ["config", "main", "run"]

[tool.setuptools.packages.find]
where = ["."]
include = ["iosrealrun*", "driver*", "init*", "util*"]

[tool.setuptools.package-data]
iosrealrun = ["data/*.yaml", "data/*.txt", "web_static/*.html", "web_static/*.css", "web_static/*.js"]

[dependency-groups]
dev = [
    "httpx>=0.27.0",
    "pytest>=8.0",
    "ruff>=0.6.0",
]

[tool.pytest.ini_options]
pythonpath = ["."]
